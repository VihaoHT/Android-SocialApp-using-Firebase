<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <item android:title="Profile Settings">
        <menu>
            <item android:title="Change Password"
                android:id="@+id/Change"
                android:icon="@drawable/padlock"
                />
            <item android:title="Change Email Address"
                android:id="@+id/ChangeEmail"
                android:icon="@drawable/mail"
                />
            <item android:title="Change Information"
                android:id="@+id/ChangeInfo"
                android:icon="@drawable/userchange"
                />
        </menu>

    </item>

    <item android:title="Logout"
        android:id="@+id/setting"
        />

</menu>